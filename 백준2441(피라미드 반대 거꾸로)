#include <stdio.h>
int main() {
	int i, a, b;
	scanf("%d", &i);
	for (a = 1; a <= i; a++) {
		for (b = 1; b <= a - 1; b++) {
			printf(" ");
		}
		for (b = 1; b <= i - a + 1; b++) {
			printf("*");
		}
		puts("");

	}
	
	return 0;
}
